name: sawmill_tracking
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  flutter_barcode_scanner: 
   git:
    url: https://github.com/irawanmurjayanto/barcode_modi2.git        
  flutter_easyloading: ^3.0.5
  get: ^4.7.2
  get_storage: ^2.1.1
  http: ^1.3.0
  intl: ^0.20.2
  provider: ^6.1.2
     

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
  - assets/images/
  - assets/logos/
